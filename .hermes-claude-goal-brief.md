/goal Two-part upgrade to p2pkit and the messaging app so peering works without the WebSocket lobby after bootstrap. Work /root/work/p2pkit (branch master at tip 10d7055 or later; verify clean; commit to a new branch feat/mesh-signalling) and /root/work/messaging (branch main at 8b196c9; verify clean except .hermes-claude-goal-brief.md, never commit it). Use /opt/node22/bin. NO push/deploy/live services. p2pkit repo is the user's own public fork — patching core is authorized product work; keep changes minimal and idiomatic to the existing design.

PART A — enable GossipDiscovery in messaging (small, do first):
1. In /root/work/messaging/packages/client/src/messaging.ts (~line 558) and packages/node/src/archivist.ts (~line 128): construct P2PKit with `discovery: new GossipDiscovery()` (import from the pinned p2pkit export surface — check /root/work/messaging/node_modules/p2pkit/dist/index.d.ts for the exact export name; if the pin predates the export, repin messaging deps.p2pkit to the p2pkit commit you produce in PART B/C and npm install).
2. Prove it: out-of-band test (scratch dir under /root/.hermes/cache/scratch/gossip-proof/) that starts the signalling server + TWO wrtc-based node clients (or reuse existing test helpers from /root/work/p2pkit/test/helpers), lets A and B connect, then KILLS the signalling server, then starts client C with the signalling URL pointed at a dead port. Expected: C never learns A/B via WS (dead), so gossip alone can't bootstrap C — instead prove the mesh-heal half: with A+B linked and signalling dead, a NEW peer address injected via gossip (simulate by connecting C to A through A's still-alive slot while C uses the dead lobby for its own kit → simplest deterministic proof: A and B each constructed with GossipDiscovery; after A-B link forms, stop signalling; then B dials a third kit instance created earlier but not yet started... if the deterministic proof is awkward, at minimum assert gossip frames flow: A and B exchange peer lists on link formation, observable via p2pkit debug events or by asserting kit.peerIds() on both sides after killing signalling and forcing a reconnect cycle). Report honestly what was proven and what could not be without brokered signalling (PART C).

PART B — peering abstraction (medium):
3. P2PKit options already accept `signalling` and `discovery`. Add a small facade so consumers don't construct transports: e.g. `P2PKit.bootstrap({ lobby?: url, discovery?: ... })` or accept `bootstrap: { kind: "lobby", url } | { kind: "url-list", urls }` — pick the smallest change that lets messaging pass a lobby URL instead of importing WebSocketSignalling. Migrate messaging client+archivist+signalling package consumers to it. Keep backward compat for existing `signalling:` option (p2pkit is public; don't break the API surface) — deprecate in docs/README rather than remove.

PART C — peer-brokered signalling (large, the core feature):
4. Design: a frame `k: "sig-relay"` carried over EXISTING peer links, addressed {to: peerId, payload: <exactly the lobby's signal envelope: announce/description/iceCandidate>}. When a kit has no live lobby (or as a supplement), outbound handshake signals to a peer we know-but-aren't-linked-to are sent to any connected peer with a request to forward; receiving kits deliver to their signalling handler as if from the lobby, and route replies back the same way. TTL/hop-count 1 (only direct neighbors forward; no flooding). Dedup by signal id. The broker is a postman: payload bytes stay the same direct-only SDP/candidates; the no-TURN validation in transports/rtc.ts stays.
5. Gate it behind an option (`brokeredSignalling: true` default OFF; messaging turns it ON). When enabled AND the lobby is down, known-peer addresses from gossip become dialable: the client asks a linked peer to carry its offer.
6. Tests in /root/work/p2pkit/test/ (vitest, follow existing style in mesh.test.ts / discovery.test.ts): (a) three-node test — A-B and B-C linked, no lobby; A can complete a handshake to C via B relaying; assert A-C direct link exists and its data channel passes frames (b) lobby-up parity: brokered path doesn't disturb normal lobby signalling (c) TTL: relayed signals are not re-forwarded (d) dedup: duplicate relay dropped (e) broker leaves: pending relay times out cleanly, no crash. Run `npm test` in p2pkit — all existing 96+ tests must still pass.
7. Messaging proof for the whole chain (out-of-band, scratch): two wrtc clients + archivist via local lobby; then kill the lobby; then a third client with gossip+brokeredSignalling enabled, seeded with one known address (e.g. via the registry topic or a direct connect to A), completes a handshake to B through A while the lobby is dead, and sends/receives a room message end-to-end. This is the acceptance for the feature.

Rules: no new runtime deps; no TURN/relay candidates anywhere; mesh frames stay bounded (reuse chunking limits); do not touch /root/work/webui; do not modify chat.yazdani.au or the deployed stack. Write progress to /root/.hermes/cache/scratch/mesh-signalling.status.json (phase/updated_at/detail, secret-free) before each phase. Verify: p2pkit `npm run build && npm test` all green; messaging `npm run build && npm run typecheck && npm test && npm run smoke` green with the repinned p2pkit (commit SHA pin, not branch); then /tmp/messaging-acceptance/acceptance.mjs --local 15/15. Report: branches/commits, exact p2pkit API additions, test list with counts, the end-to-end proof transcript summary, and any deviation with one-line reason.